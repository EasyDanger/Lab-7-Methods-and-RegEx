# Lab 7

Version History
0.3--10/15/2018--Initial commit, method logic written, no UI.
1.0--10/16/2018--UI added, method for finding validation type from input added.
	Known Issues:
		forced to remove buggy expression for first and last names.
		still no support for HTML elements.
		New method to try validation types in absence of choice from user breaks rules followed by other methods (e.g. returns Strings, not booleans;  
		More testing needed.
		code cleanup.
1.2--10/16/2018--Comments added. Code cleanup/formatting.
	Known Issues:
		Still no support for HTML elements
		more testing needed.
